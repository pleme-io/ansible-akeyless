---
# Integration test for dynamic_secret_gcp

- name: Test dynamic_secret_gcp module
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Create dynamic_secret_gcp
      drzln0.akeyless.dynamic_secret_gcp:
        state: present
        name: "test_value"
      register: create_result

    - name: Verify creation
      ansible.builtin.assert:
        that:
          - create_result.changed

    - name: Create dynamic_secret_gcp (idempotent)
      drzln0.akeyless.dynamic_secret_gcp:
        state: present
        name: "test_value"
      register: idempotent_result

    - name: Delete dynamic_secret_gcp
      drzln0.akeyless.dynamic_secret_gcp:
        state: absent
        name: "test_value"
      register: delete_result

    - name: Verify deletion
      ansible.builtin.assert:
        that:
          - delete_result.changed
